requests>=2.32.0
PyYAML>=6.0.1
opencv-python>=4.10.0
Pillow>=10.4.0
numpy>=1.26.0
tqdm>=4.66.0

# --- 评估用 (L1/L2) ---
scikit-image>=0.24.0          # SSIM
# torch, torchmetrics, lpips   # 需要 LPIPS 时再装, 体积较大
# insightface, onnxruntime     # 需要人脸身份相似度时再装

# --- 开发 ---
pytest>=8.3.0
ruff>=0.6.0
