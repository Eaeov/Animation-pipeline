# TASK.md — 原始题面与约束（不可修改）

> 本文件是**题面原文归档 + 不可协商的约束**。AI Agent 在做出任何设计决策前，
> 必须回来对照本文件确认没有偏离题目要求。
> 本文件只在题面发生变化时修改，平时只读。

---

## 1. 题面原文

**题 C：动画AI管线方向**

> 任务：任选今年新番动画的一个宣传PV，截取若干片段，使用标准日式人设图素材进行人物替换，
> 不限制 pipeline 方式，可以通过视频生成模型也可以通过图片生成模型。
>
> - 侧重对结果质量的评估，不侧重工作量（不关注质量将整个 PV 都跑了也没有意义）
> - 侧重 pipeline 的设计和评估
> - 侧重 pipeline 的可复用和稳定性，对片段 A 和片段 B 用同样的 pipeline 都跑出 75 分的效果
>   > 对片段 A/片段 B 特调出 90 分的效果

**交付：**

1. 公开的 GitHub 源码仓库
2. 一个 Markdown 简要说明：
   - 实现方式以及怎么规划时间
   - 对 pipeline 设计的考量和侧重点
   - 对结果评估方案的设计和考量
   - 用了什么 ai coding 工具，大概花了多少 token，大概什么部分花了最多的 token
   - 你自己个人时间花最多的部分（不算使用 LLM 直接做的部分）
   - 失败案例分析：哪些地方做错/做不了，原因是什么

---

## 2. 关键词逐条拆解（很多坑藏在这里）

| 题面词 | 含义 | 落地方案 | 易错点 |
|---|---|---|---|
| **今年新番** | 2026 年度播出的动画 | 需在 README 明确标注作品名 + 放送年份 + PV 来源链接 | 用了老番 / 没标年份 |
| **宣传 PV** | 官方预告片，通常 30s–2min | 记录原始时长、来源 URL | 用了正片片段 |
| **截取若干片段** | ≥2 个，建议 3 个（含 1 个反例） | A/B 必做，C 作为压力测试 | 只做 1 个 → 无法验证 P1 |
| **标准日式人设图素材** | 典型动漫角色立绘（非真人、非写实） | 记录素材来源与授权说明 | 用真人照 → 跑偏 |
| **人物替换** | 换掉片中主角，保留场景动作 | wan2.2-animate-mix（Mix 模式） | 用 Move 模式（会换背景） |
| **不限制 pipeline 方式** | 可用视频模型或图片模型 | 我们选视频模型路线，但保留 adapter 扩展位 | — |
| **侧重质量评估** | 评估方案是核心交付物 | 三层评估体系（见 docs/03） | 只出片不出报告 |
| **不侧重工作量** | 跑满全片**减分** | 只跑精选片段 | 贪多跑全 PV |
| **可复用和稳定性** | **本题最高优先级** | A/B 同 pipeline + 跨片段方差指标 | 为单片段特调 |
| **75分 vs 90分** | 明确的价值取向 | 必须能报出"A/B 一致性"这个数 | 只报单片段最好成绩 |

---

## 3. 硬约束（违反即失分）

### 3.1 来自题面

- [ ] 必须是**今年（2026）新番**的**宣传 PV**
- [ ] 必须**截取片段**（不能整片）
- [ ] 必须用**日式人设图**做**人物替换**
- [ ] 必须覆盖**至少 A、B 两个片段**，且**用同一 pipeline**
- [ ] 必须交付**评估方案与结果**（不是可选）
- [ ] 必须交付**失败案例分析**（题面明确列出）

### 3.2 来自平台（百炼 API）

- [ ] 参考视频时长 **2s < d ≤ 30s**，超 30s 必须再切
- [ ] 参考视频宽高 **200–2048px**，宽高比 **1:3–3:1**
- [ ] 参考视频 ≤ 200MB，格式 MP4/AVI/MOV
- [ ] 人设图宽高 200–4096px，宽高比 1:3–3:1，≤5MB
- [ ] 输入素材需**公网可访问 URL**（或走 OSS 中转）
- [ ] 结果 URL 有效期 24h，**必须及时下载转存**
- [ ] 图片需通过 `check_image` 合规检测（单人、面部清晰、无遮挡）

### 3.3 来自工程（自我约束）

- [ ] 不提交任何密钥
- [ ] 不提交大视频（用 LFS 或外链）
- [ ] A/B 走**完全同构**的目录与代码路径

---

## 4. 素材版权与合规声明要求

PV 与人设图均为第三方版权素材。仓库中必须包含 `docs/素材来源与授权.md`，记录：

| 字段 | 说明 |
|---|---|
| 作品名 / 放送季度 | 如「XXX」2026 年 4 月新番 |
| PV 来源 URL | YouTube / 官网 / B站 官方号 |
| 人设图来源 | 官方立绘 / 公开素材站，注明许可 |
| 使用范围声明 | 仅用于技术作业演示，不用于商业用途，不主张任何权利 |
| 下载时间 | 便于溯源 |

> **反例**：仓库里放了大段原片却没写来源 → 合规风险 + 显得不专业。

---

## 5. 验收清单（结题前逐条打勾）

**交付物**
- [ ] GitHub 公开仓库可访问，README 有快速开始
- [ ] `docs/` 下需求分析、技术选型、Pipeline 设计、评估方案、交付说明齐全
- [ ] 结题 Markdown 含题面要求的 6 个问题，**逐条回答**
- [ ] `docs/素材来源与授权.md` 存在

**Pipeline**
- [ ] A、B 两片段均产出成片
- [ ] A、B 使用**同一套配置与代码路径**（可用 diff 证明）
- [ ] 五个阶段可独立运行，manifest 完整
- [ ] Mock adapter 可离线跑通全链路

**评估**
- [ ] L1 客观指标有数值
- [ ] L2 语义指标有 VLM 评分 + 证据图
- [ ] **L3 跨片段方差有数值** ← 本题核心
- [ ] 有失败案例归因（可复现的错误样本）

**诚实性**
- [ ] token 消耗与时间投入如实记录
- [ ] 失败案例不美化，写清"做不了"的原因
