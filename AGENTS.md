# AGENTS.md — AI 协作入口（vibecoding 框架核心）

> 任何人（或任何 AI Agent）接手本仓库，**先读这个文件**，再读 `docs/` 下的对应文档。
> 本文件是"任务说明书 + 施工规范"，目标是让 AI 在 **5 分钟内**理解任务边界并开始产出。

---

## 0. 一句话任务

**取今年新番动画的一支宣传 PV，切片后用标准日式人设图把片中角色「换」掉，产出一套稳定、可复用、可评估的动画 AI 管线（pipeline），并对结果质量给出量化评估。**

评估权重：**Pipeline 设计与可复用/稳定性 > 单片段特调质量 > 工作量**。

---

## 1. 三条不可违背的判分原则（读三遍）

| # | 原则 | 反面案例（绝对不要做） |
|---|---|---|
| P1 | **可复用 > 高分** | 对 A 片段特调 prompt/参数拿到 90 分，B 片段崩掉 → **这题就废了** |
| P2 | **质量 > 工作量** | 把整支 PV 全跑一遍但没做任何评估 → **没有意义** |
| P3 | **评估方案是交付物本身** | 只出视频不出评估报告 → 不完整 |

> 判分口径：对片段 A 和片段 B 用**同一套 pipeline** 都跑到 75 分，**优于** 为 A/B 分别特调到 90 分。

---

## 2. 技术选型结论（已定，不要随意改动）

**核心模型：阿里云百炼 `wan2.2-animate-mix`（万相-视频换人）**

选它的理由（为什么不是纯 I2V）：

- **天然保留原 PV 的运镜、节奏、光影** —— 它做的是"替换主角"，不是"重新生成画面"。对"人物替换"这个任务语义最贴合。
- **可控性高**：输入 = 1 张人设图 + 1 段参考视频，输出变量少，**跨片段稳定性天然好**（正中 P1）。
- 对比纯首帧 I2V：I2V 会重绘背景，跨片段风格漂移严重，难保证 A/B 同质。

**硬约束（决定 pipeline 必须先切片）：**

| 输入 | 约束 |
|---|---|
| 参考视频 | 200–2048px 宽高、宽高比 1:3–3:1、**2s < 时长 ≤ 30s**、≤200MB、MP4/AVI/MOV |
| 人设图 | 200–4096px、宽高比 1:3–3:1、≤5MB、JPG/PNG/BMP/WEBP |
| 输出 | 720P；`wan-std` 15fps / `wan-pro` 25fps |

**计费与额度（影响实验规划）：**

- 免费额度：标准+专业模式**共 50 秒**；图片检测 200 张免费
- 超出：std 0.6 元/秒，pro 0.9 元/秒
- 输出计费，输入不计费；失败不扣费

→ **策略：std 模式跑通全流程 → pro 模式只跑最终验收片段。**

完整选型对比见 `docs/01-技术选型.md`。

---

## 3. Pipeline 五阶段（架构冻结）

```
[1] ingest      原始 PV → 切片 + 归一化（分辨率/帧率/时长/编码）
[2] character   人设素材 → 合规检测 + 规范化
[3] style_align (可选) 人设图与目标片段画幅/身材比例对齐
[4] transfer    wan2.2-animate-mix 换人生成
[5] assemble    回拼 + 转场 + 交付成片
        ↕
   [eval] 贯穿全程 → 自动指标 + VLM 评分 + 人工盲评
```

**设计要点：阶段之间通过文件系统交换产物（manifest），不允许上游直接改下游状态** —— 这是保证「可复用」的工程基础。

详细设计见 `docs/02-Pipeline设计.md`。

---

## 4. 代码规范（AI 生成代码时必须遵守）

```
src/anime_pv/
├── stages/       # 五个阶段，每个 stage 一个模块，统一实现 Stage 协议
├── adapters/     # 外部模型适配器（DASHSCOPE / MOCK），换模型不改 stages
├── eval/         # 评估器，每个评估维度一个 Evaluator
├── utils/        # 视频/图片/IO/日志 工具
└── cli.py        # 统一命令行入口
```

**铁律：**

1. **Stage 必须可独立运行**（`python -m anime_pv.cli run --stage transfer --clip A`），禁止阶段间隐式依赖。
2. **所有外部模型调用走 Adapter 抽象**。新增模型 = 新增 adapter 文件，**不许改 stages/**。
   - 已有：`DashScopeAnimateMixAdapter`（真实）、`MockAdapter`（离线跑通用，返回占位产物）
3. **配置外置**：所有魔法数字进 `configs/*.yaml`，代码里禁止硬编码 prompt/分辨率/时长。
4. **产物落盘规范**：`runs/<run_id>/<clip_id>/<stage>/`，每步写 `manifest.json`（含输入 hash、参数、耗时、模型版本、输出路径）。
5. **失败必须可诊断**：异常统一抛 `PipelineError`，附带 stage / clip / 百炼 request_id。
6. **幂等**：同 run_id 重跑不重复扣费（先查 manifest 若已存在则跳过，`--force` 覆盖）。
7. 禁止提交任何 API Key。密钥只从环境变量 `DASHSCOPE_API_KEY` 读取。

---

## 5. 评估方案（P3 交付物，按此实现）

**三层评估，缺一不可：**

| 层 | 指标 | 工具 | 目的 |
|---|---|---|---|
| L1 技术质量 | PSNR / SSIM / LPIPS / 时序闪烁度 / 光流平滑度 | OpenCV / torchmetrics | 客观、可复现 |
| L2 语义质量 | 身份一致性 / 动作还原度 / 场景保留度 / 伪影 | VLM（qwen-vl-max）+ 人脸相似度 | 对齐"换人"任务本质 |
| L3 稳定性 | **跨片段方差**（A/B 用同 pipeline 的分数标准差） | 自研 | **直接回答 P1** |

**关键设计：L3 是本题的胜负手。** 只报单片段高分不作数，必须报"同一 pipeline 在 A/B/C 片段上的得分方差"。

详细方案见 `docs/03-评估方案.md`。

---

## 6. 时间规划（3 小时）

| 时段 | 任务 | 产出 |
|---|---|---|
| 0:00–0:20 | 技术选型 + 框架搭建 | 本文件 + 骨架 |
| 0:20–0:50 | 需求分析文档 | `docs/00-需求分析.md` |
| 0:50–1:30 | 素材准备（PV 切片 + 人设图） | `assets/` 就位 |
| 1:30–2:20 | Pipeline 打通（std 模式） | A/B 片段出片 |
| 2:20–2:45 | 评估执行 + 出报告 | `runs/*/eval_report` |
| 2:45–3:00 | pro 模式终稿 + 文档收尾 | 交付 |

（详细排期与关键路径见 `docs/00-需求分析.md`）

---

## 7. AI Agent 工作约定

- **改动前先读** `docs/02-Pipeline设计.md` 和对应 stage 的现状，禁止凭想象重构。
- **新增模型** → 只动 `adapters/`；**新增指标** → 只动 `eval/`；**两者都不许动 `stages/` 的核心流程**。
- **不确定素材/密钥** → 用 `MockAdapter` 先跑通链路，不要卡住。
- **每次改动后**更新 `docs/CHANGELOG.md`，格式：`[stage] 做了什么 / 为什么`。
- **不要做的事**：把整支 PV 全量跑、为单个片段调参到过拟合、在代码里写死某个片段的专属 prompt。

---

## 8. 快速开始

```bash
# 1. 装依赖
pip install -r requirements.txt

# 2. 配密钥
export DASHSCOPE_API_KEY=sk-xxxx

# 3. 离线验证链路（不花钱）
python -m anime_pv.cli smoke --adapter mock

# 4. 跑一个片段
python -m anime_pv.cli run --clip A --adapter dashscope --mode wan-std

# 5. 出评估报告
python -m anime_pv.cli eval --run-id <run_id>
```

---

## 9. 文档索引

| 文件 | 内容 |
|---|---|
| `docs/00-需求分析.md` | 任务拆解、约束、验收标准、时间规划 |
| `docs/01-技术选型.md` | 模型/工具选型对比与决策依据 |
| `docs/02-Pipeline设计.md` | 五阶段详细设计、数据结构、接口 |
| `docs/03-评估方案.md` | 三层评估体系与打分细则 |
| `docs/04-交付说明.md` | 交付物清单 + 结题报告（含失败案例） |
| `docs/CHANGELOG.md` | 变更记录 |
